using UnityEngine;
//CLASE PARA MANEJAR EL ESTADO DE ARRASTRE AL INICIAR
//esto es como el estado de que esta siewndo arrastrado desde el bench (donde se guarda el moñeco)
public class DraggingStart : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
