using UnityEngine;
//iNTERFACE PARA LOS ESTADOS DE LAS UNIDADES, SE USARÁ PARA LOS ENEMIGOS Y PARA EL JUGADOR, ASÍ SE PODRÁ CONTROLAR MEJOR EL COMPORTAMIENTO DE CADA UNO
public class IUnitState : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
